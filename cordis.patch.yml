# Cannot rename a row: loader skips `name` mismatches.
# Disable stock basic, then insert this engine as a new host-plane row.
- id: compaction-basic
  disabled: true
- insert:
    - id: compaction-smart
      name: dsh-smart-compaction
