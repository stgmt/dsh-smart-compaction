# Host plane only. The engine is NOT inserted here — ctx.compaction lives in
# each agent preset's isolate group. Autonomy rewrites those rows in place so
# the user keeps their current mode (standard, creator, reels-dsh, …).
- id: compaction-basic
  disabled: true
- insert:
    - id: smart-compaction-autonomy
      name: dsh-smart-compaction/autonomy
